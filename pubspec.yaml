name: imob
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  imob_design_system:
    path: ./imob_design_system
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

  provider: ^5.0.0

  firebase_core: ^1.3.0
  firebase_auth: ^1.4.1
  cloud_firestore: ^2.2.2

  mask_text_input_formatter: ^2.0.0
  smooth_page_indicator: ^0.3.0-nullsafety.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - assets/images/