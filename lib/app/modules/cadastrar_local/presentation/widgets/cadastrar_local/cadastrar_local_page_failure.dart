import 'package:flutter/material.dart';

class CadastrarLocalPageFailure extends StatelessWidget {
  const CadastrarLocalPageFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
